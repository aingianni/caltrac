import NewDietForm from "../../components/NewDietForm/NewDietForm"

export default function DashBoardPage ({ user, setUser }) {

    return (
      <main>
        <h1>This is the User Dashboard</h1>
        {/* <NewDietForm user={user} setUser={setUser} /> */}
      </main>
    )
  }